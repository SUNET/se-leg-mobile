/**
 * Sender component custom factory (generated from Gulp).
 * @param {type} angular
 * @author Alejandro Gomez <amoron@emergya.com>
 * @since Jan 27 2017
 */
(function () {
  define(['./sender.module'], function (moduleName) {
    'use strict';

    angular
      .module(moduleName)
      .factory('SenderCustomProcessDataFactory', SenderCustomProcessDataFactory);

    /* @ngInject */
    function SenderCustomProcessDataFactory(UtilsFactory, DataFactory, SE_LEG_VIEWS) {
      var factory = this;

      factory.getProcessedData = getProcessedData;
      factory.getCustomProcessedData = getCustomProcessedData;

      /**
       * It retrieves the processed data to be sent.
       * @return the processed data.
       */
      function getProcessedData() {
        var data = getCustomProcessedData();
        // if it's undefined, it's because the metho wasn't created from gulp
        if (data === undefined) {
          data = UtilsFactory.jsonToQueryString(DataFactory.getAll());
        }
        return data;
      }

      /**
       * Autogenerated method from gulp.
       * @returns the processed data.
       */
      function getCustomProcessedData() {
        /** @@get-custom-processed-data-placeholder */
      }

      return factory;

    }
  });
})();
