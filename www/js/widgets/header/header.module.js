/**
 * Se-leg header
 * AngularJS module configuration.
 * @returns {angular.module}
 * @author Ignacio González <igonzalez@emergya.com>
 */
(function () {

  define([
    'angular'
  ], function (ng) {
    'use strict';

    var moduleName = 'app.widgets.seLegHeader';

    ng.module(moduleName, []);

    return moduleName;
  });
})();
